import { Head, Link, router } from '@inertiajs/react'
import { IParaclinicRequest } from '@/interfaces/IParaclinicRequest'
import { ArrowLeft, FileText, Image, File, Upload, Trash2, Calendar } from 'lucide-react'
import { useState } from 'react'
import { Alert, Box, Button, Card, CardContent, Chip, Divider, IconButton, Stack, Table, TableBody, TableCell, TableHead, TableRow, TextField, Typography } from '@mui/material'
import { useModal } from '@/components/modal'
import { useToast } from '@/components/toast'
const STATUS_COLORS: Record<string, 'default' | 'primary' | 'error' | 'info' | 'success' | 'warning'> = { Draft: 'default', Requested: 'info', 'Waiting Result': 'warning', 'Result Received': 'success', Reviewed: 'primary', Completed: 'success', Cancelled: 'error' }
const PAYMENT_COLORS: Record<string, 'default' | 'primary' | 'error' | 'info' | 'success' | 'warning'> = { Unpaid: 'error', Partial: 'warning', Paid: 'success' }
const STATUS_TRANSITIONS: Record<string, string[]> = { Draft: ['Requested', 'Cancelled'], Requested: ['Waiting Result', 'Cancelled'], 'Waiting Result': ['Result Received', 'Cancelled'], 'Result Received': ['Reviewed', 'Completed'], Reviewed: ['Completed'], Completed: [], Cancelled: [] }
const fileIcon = (mime: string) => mime.startsWith('image/') ? <Image size={20} color="#1976d2" /> : mime.includes('pdf') ? <FileText size={20} color="#d32f2f" /> : <File size={20} color="#757575" />
const formatSize = (bytes: number) => bytes < 1024 ? `${bytes} B` : bytes < 1024 * 1024 ? `${(bytes / 1024).toFixed(1)} KB` : `${(bytes / (1024 * 1024)).toFixed(1)} MB`
const Show = ({ request }: { request: IParaclinicRequest }) => {
   const { openAlert } = useModal(); const { toast } = useToast(); const [isUploading, setIsUploading] = useState(false); const [resultSummary, setResultSummary] = useState(request.results?.[0]?.result_summary ?? ''); const [doctorInterpretation, setDoctorInterpretation] = useState(request.results?.[0]?.doctor_interpretation ?? '')
   const handleUpload = (e: React.ChangeEvent<HTMLInputElement>) => { const file = e.target.files?.[0]; if (!file) return; setIsUploading(true); const formData = new FormData(); formData.append('file', file); router.post(`/paraclinic-requests/${request.id}/attachments`, formData, { onSuccess: () => toast('File uploaded successfully!', { variant: 'success' }), onError: (err) => toast(Object.values(err).join(', ') || 'Upload failed', { variant: 'error' }), onFinish: () => setIsUploading(false) }); e.target.value = '' }
   const handleDeleteAttachment = (a: { id: number; file_name: string }) => openAlert({ message: `Delete ${a.file_name}?`, description: 'This action cannot be undone.', variant: 'danger', confirmLabel: 'Delete', onConfirm: () => router.delete(`/paraclinic-requests/${request.id}/attachments/${a.id}`) })
   const handleStatusChange = (status: string) => router.patch(`/paraclinic-requests/${request.id}/status`, { status }, { onSuccess: () => toast(`Status updated to ${status}`, { variant: 'success' }) })
   const transitions = STATUS_TRANSITIONS[request.status] ?? []
   return <Box sx={{ minHeight: '100vh', bgcolor: 'background.default' }}><Head title={`Request ${request.request_number}`} /><Card square sx={{ position: 'sticky', top: 0, zIndex: 1 }}><Stack direction={{ xs: 'column', md: 'row' }} spacing={2} sx={{ p: 2, alignItems: { md: 'center' } }}><Link href="/paraclinic-requests"><IconButton aria-label="Back to paraclinic requests"><ArrowLeft size={20} /></IconButton></Link><Box sx={{ flex: 1 }}><Stack direction="row" spacing={1} sx={{ flexWrap: 'wrap', alignItems: 'center' }}><Typography variant="h5" sx={{ fontWeight: 700 }}>{request.request_number}</Typography><Chip size="small" label={request.status} color={STATUS_COLORS[request.status] ?? 'default'} /><Chip size="small" label={request.payment_status} color={PAYMENT_COLORS[request.payment_status] ?? 'default'} /></Stack><Typography variant="body2" color="text.secondary">{request.request_date} · Created by {request.created_by ?? 'N/A'}</Typography></Box><Stack direction="row" spacing={1} sx={{ flexWrap: 'wrap' }}>{request.status !== 'Completed' && request.status !== 'Cancelled' && <>{request.status === 'Waiting Result' && <Button component="label" variant="contained" startIcon={<Upload size={16} />} disabled={isUploading}>Upload Result<input hidden type="file" onChange={handleUpload} disabled={isUploading} /></Button>}{transitions.map((s) => <Button key={s} variant="outlined" onClick={() => handleStatusChange(s)}>Mark as {s}</Button>)}</>}<Link href={`/paraclinic-requests/${request.id}`}><Button variant="outlined">Print</Button></Link></Stack></Stack></Card><Stack spacing={3} sx={{ p: { xs: 2, md: 4 }}}><Section title="General Information"><Stack direction={{ xs: 'column', md: 'row' }} sx={{ flexWrap: 'wrap', gap: 3 }}>{[['Patient', request.patient ? `${request.patient.khmer_last_name} ${request.patient.khmer_first_name}` : null], ['Doctor', request.doctor?.name ?? null], ['External Facility', request.external_facility_name], ['Request Date', request.request_date], ['Provisional Diagnosis', request.provisional_diagnosis]].map(([label, value]) => <InfoItem key={label as string} label={label as string} value={value as string | null} />)}</Stack>{request.clinical_reason && <InfoItem label="Clinical Reason" value={request.clinical_reason} />}{request.notes && <InfoItem label="Notes" value={request.notes} />}</Section><Section title={`Diagnostic Tests (${request.tests.length})`}>{request.tests.length === 0 ? <Typography color="text.secondary">No tests added.</Typography> : <Table><TableHead><TableRow>{['Category', 'Test Name', 'Priority', 'Instruction'].map((h) => <TableCell key={h}>{h}</TableCell>)}</TableRow></TableHead><TableBody>{request.tests.map((t, i) => <TableRow key={t.id ?? i}><TableCell>{t.test_category}</TableCell><TableCell>{t.test_name}</TableCell><TableCell><Chip size="small" label={t.priority} color={t.priority === 'STAT' ? 'error' : t.priority === 'Urgent' ? 'warning' : 'default'} /></TableCell><TableCell>{t.instruction ?? '—'}</TableCell></TableRow>)}</TableBody></Table>}</Section><Section title="Attachments"><Button component="label" variant="contained" startIcon={<Upload size={16} />} disabled={isUploading}>Upload File<input hidden type="file" onChange={handleUpload} disabled={isUploading} /></Button><Typography variant="caption" sx={{ display: 'block' }} color="text.secondary">Max file size: 20 MB</Typography><Stack spacing={1} sx={{ mt: 2 }}>{request.attachments.length === 0 ? <Typography color="text.secondary" sx={{ textAlign: 'center' }}>No files uploaded yet.</Typography> : request.attachments.map((a) => <Stack key={a.id} direction="row" spacing={1.5} sx={{p: 1.5, border: 1, borderColor: 'divider', borderRadius: 1, alignItems: 'center'}}>{fileIcon(a.mime_type)}<Box sx={{ flex: 1, minWidth: 0 }}><Typography noWrap sx={{ fontWeight: 600 }}>{a.file_name}</Typography><Typography variant="caption" color="text.secondary">{formatSize(a.file_size)}{a.uploaded_by && ` • by ${a.uploaded_by}`}</Typography></Box><Button component="a" href={`/paraclinic-requests/attachments/${a.id}/view`} target="_blank" rel="noopener noreferrer">View</Button><IconButton onClick={() => handleDeleteAttachment(a)} aria-label={`Delete ${a.file_name}`} color="error"><Trash2 size={16} /></IconButton></Stack>)}</Stack></Section><Section title="Results & Interpretation">{request.results.map((r) => <Alert key={r.id} severity="info" sx={{ mb: 1 }}><Typography variant="caption"><Calendar size={12} /> {r.result_date} · Reviewed by {r.reviewed_by ?? 'N/A'}{r.reviewed_at && ` at ${new Date(r.reviewed_at).toLocaleString()}`}</Typography>{r.result_summary && <Typography><strong>Summary:</strong> {r.result_summary}</Typography>}{r.doctor_interpretation && <Typography><strong>Interpretation:</strong> {r.doctor_interpretation}</Typography>}</Alert>)}{(request.status === 'Waiting Result' || request.status === 'Result Received') && <Stack spacing={2} sx={{ mt: 2 }}><Divider /><Typography variant="subtitle1" sx={{ fontWeight: 600 }}>Doctor Review</Typography><TextField label="Result Summary" multiline rows={4} value={resultSummary} onChange={(e) => setResultSummary(e.target.value)} placeholder="Enter result summary..." /><TextField label="Doctor Interpretation" multiline rows={4} value={doctorInterpretation} onChange={(e) => setDoctorInterpretation(e.target.value)} placeholder="Enter interpretation..." /><Button onClick={() => handleStatusChange('Reviewed')} sx={{ alignSelf: 'flex-start' }}>Mark as Reviewed</Button></Stack>}</Section><Section title="Billing"><Stack direction={{ xs: 'column', md: 'row' }} spacing={4}><InfoItem label="Subtotal" value={`$${(request.subtotal ?? 0).toFixed(2)}`} /><InfoItem label="Discount" value={`$${(request.discount ?? 0).toFixed(2)}`} /><InfoItem label="Total Amount" value={`$${(request.total_amount ?? 0).toFixed(2)}`} /><InfoItem label="Payment Status" value={<Chip size="small" label={request.payment_status} color={PAYMENT_COLORS[request.payment_status] ?? 'default'} />} /><InfoItem label="Payment Date" value={request.payment_date} /></Stack></Section><Section title="Timeline"><Stack spacing={1}><Typography variant="body2">● Created · {new Date(request.created_at).toLocaleString()}{request.created_by && ` by ${request.created_by}`}</Typography>{request.updated_at !== request.created_at && <Typography variant="body2">● Last updated · {new Date(request.updated_at).toLocaleString()}</Typography>}</Stack></Section></Stack></Box>
}
const Section = ({ title, children }: { title: string; children: React.ReactNode }) => <Card><CardContent><Typography variant="subtitle2" color="text.secondary" sx={{mb: 2, textTransform: 'uppercase', fontWeight: 700}}>{title}</Typography>{children}</CardContent></Card>
const InfoItem = ({ label, value }: { label: string; value: React.ReactNode }) => <Box sx={{ minWidth: 160, flex: '1 1 18%', mt: 1 }}><Typography variant="caption" color="text.secondary" sx={{ fontWeight: 600 }}>{label}</Typography><Typography variant="body2" color={value === null || value === undefined ? 'text.disabled' : 'text.primary'}>{value ?? '—'}</Typography></Box>
export default Show
